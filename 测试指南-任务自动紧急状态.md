# 任务自动紧急状态功能测试指南

## 测试场景

### 场景1：添加24小时内到期的任务

1. 打开应用
2. 点击"添加任务"按钮
3. 填写任务信息：
   - 任务名称：测试紧急任务
   - 预估时间：1小时
   - 截止时间：设置为今天晚上（24小时内）
   - **不要手动勾选"紧急"选项**
4. 点击"确认"
5. **预期结果**：任务会自动显示为紧急状态（红色标记或紧急图标）

### 场景2：添加24小时后到期的任务

1. 点击"添加任务"按钮
2. 填写任务信息：
   - 任务名称：测试非紧急任务
   - 预估时间：1小时
   - 截止时间：设置为3天后
3. 点击"确认"
4. **预期结果**：任务不会显示为紧急状态

### 场景3：编辑任务截止时间

1. 找到一个非紧急任务（截止时间>24小时）
2. 点击编辑按钮
3. 将截止时间改为今天晚上（24小时内）
4. 点击"保存"
5. **预期结果**：任务自动变为紧急状态

### 场景4：移除截止时间

1. 找到一个紧急任务
2. 点击编辑按钮
3. 清除截止时间
4. 点击"保存"
5. **预期结果**：任务自动取消紧急状态

### 场景5：刷新页面后保持状态

1. 添加一个24小时内到期的任务（应自动标记为紧急）
2. 刷新页面（F5）
3. **预期结果**：任务在加载后仍然显示为紧急状态

### 场景6：定期检查（需要等待）

1. 添加一个25小时后到期的任务（不紧急）
2. 等待5分钟（应用会每5分钟自动检查）
3. 或者切换到其他应用，再切换回来（焦点事件触发检查）
4. **预期结果**：如果剩余时间已经小于24小时，任务会自动变为紧急

## 调试技巧

### 在浏览器控制台中测试

```javascript
// 查看当前所有任务
console.log(JSON.parse(localStorage.getItem('potato_tasks')))

// 手动添加一个即将到期的测试任务
const testTask = {
  id: Date.now(),
  name: '测试紧急任务',
  estimatedTime: 60,
  deadline: Date.now() + 5 * 60 * 60 * 1000, // 5小时后
  completed: false,
  cycleList: [[25, 'focus'], [5, 'rest'], [100, 'end']],
  progress: 0,
  time_up: false,
  longCycle: false,
  urgent: false, // 故意设为false，测试自动更新
  important: false,
  description: '这是一个测试任务'
}

// 添加到任务列表
const tasks = JSON.parse(localStorage.getItem('potato_tasks') || '[]')
tasks.push(testTask)
localStorage.setItem('potato_tasks', JSON.stringify(tasks))

// 刷新页面
location.reload()
```

### 检查紧急状态逻辑

```javascript
// 计算剩余时间
const task = JSON.parse(localStorage.getItem('potato_tasks'))[0]
const remainingTime = task.deadline - Date.now()
const hours = remainingTime / (1000 * 60 * 60)
console.log(`剩余时间: ${hours.toFixed(2)} 小时`)
console.log(`应该紧急: ${hours < 24}`)
```

## 预期行为总结

✅ **自动设为紧急**的情况：
- 剩余时间 < 24小时
- 已超时的任务（负数剩余时间）

❌ **不会自动设为紧急**的情况：
- 剩余时间 ≥ 24小时
- 没有截止时间的任务
- 已完成的任务

🔄 **自动检查时机**：
- 添加新任务时
- 编辑任务截止时间时
- 页面加载/刷新时
- 每5分钟（定时器）
- 窗口获得焦点时

## 常见问题

### Q: 我添加了任务但没有自动标记为紧急？
A: 请检查：
1. 截止时间是否确实在24小时内
2. 浏览器控制台是否有错误
3. 尝试刷新页面

### Q: 如何查看任务的实际截止时间？
A: 在浏览器控制台中运行：
```javascript
const tasks = JSON.parse(localStorage.getItem('potato_tasks'))
tasks.forEach(t => {
  const deadline = new Date(t.deadline)
  console.log(`${t.name}: ${deadline.toLocaleString()} (${t.urgent ? '紧急' : '正常'})`)
})
```

### Q: 定时检查是否在工作？
A: 可以在控制台添加日志：
```javascript
// 每5分钟应该会触发一次检查
// 可以在App.vue的checkTasksUrgency函数中添加console.log来验证
```
